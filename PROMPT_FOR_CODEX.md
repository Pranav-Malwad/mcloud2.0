Read ALL files inside /ai-guidelines including:

- AI_DENSITY_AND_LAYOUT_RULES.md

Your previous redesigns are still too vertically stacked and dashboard-oriented.

The UI still wastes too much vertical space and behaves like a document page instead of an operational workspace.

Refactor the Accounts module again using:

- split layouts
- workspace-style architecture
- compact density
- side detail panels
- sticky toolbars
- tabbed related data
- reduced card heights
- multi-column layouts

Primary goal:
maximize information visibility inside a single viewport with minimal scrolling.

Do NOT create large stacked card sections.

Do NOT create decorative dashboard spacing.

The layout should feel like:
Salesforce workspace + Linear operational density.